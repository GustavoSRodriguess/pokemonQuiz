import 'package:flutter/material.dart';

class quizPokemon extends StatelessWidget {
  const quizPokemon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
